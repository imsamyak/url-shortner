{
  "name": "url-shortener-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run build:packages && pnpm --filter server build && pnpm --filter client build",
    "build:packages": "pnpm -r --filter @app/* --if-present build",
    "build:server": "pnpm run build:packages && pnpm --filter server build",
    "dev": "pnpm run build:packages && pnpm --parallel --filter client --filter server dev",
    "infra:platform:synth": "pnpm --filter platform infra:synth",
    "test": "pnpm run build:packages && pnpm --filter server test",
    "typecheck": "pnpm run build:packages && pnpm -r --if-present typecheck"
  },
  "packageManager": "pnpm@11.4.0"
}
